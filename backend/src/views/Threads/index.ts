import { getThreads } from './getThreads';
import { getThread } from './getThread';
import { postComment } from './postComment';
import { postRection } from './postRection';
import { postThread } from './postThread';

export default {
  getThreads,
  getThread,
  postComment,
  postRection,
  postThread,
};
